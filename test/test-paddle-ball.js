const chai = require('chai').assert;
const Ball = require('../lib/ball');
const Paddle = require('../lib/paddle');

describe('Ball to Paddle', function() {
  it.skip('should detect a collision when the ball touches the paddle', function() {

  });
  it.skip('should invert the Y when the ball collides with the paddle', function() {

  });
});
