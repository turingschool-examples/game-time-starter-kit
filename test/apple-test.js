  const assert = require('chai').assert;
  const Apple = require('../lib/apple');

  describe('apple', function() {
    context('with default attributes', function() {
//       var apple = new Apple();
//       // it('should assign a random x coordinate', function() {
//       //   assert.equal(apple.x, 5//this needs to be changes to random);
//       // });
//       //
//       // it('should assign a random y coordinate', function() {
//       //   assert.equal(apple.y, 5//this nees to be changed to random);
//       // });
//
//       it('should assign a height', function(){
//         assert.equal(apple.height, 10);
//       });
//
//       it('should assign a width', function(){
//         assert.equal(apple.width, 10);
//       });
//     });
//
//     context('changing coordinates', function() {
//
//
  });
});

//Other modules this things needs?
//Game rules - Module
  // What are the rules for a game ending?
    //Apple hits the side of the world
    //Snake hits part of it self

//Game - Module - Ties everything together
  //This is going to be hard to test
  //Best to do last
  //Don't worry about testing this for now
  //Talk to instructors
